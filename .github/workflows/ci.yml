name: 🌊 FlexAligner CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libsndfile1

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        # 强制安装兼容版本，解决 transformers 冲突
        pip install "huggingface-hub<1.0"
        pip install .[test]
        # 补全生成模拟数据和音频处理所需的库
        pip install numpy scipy soundfile hf_transfer

    - name: Generate Mock Assets for CI
      run: |
        mkdir -p assets/examples
        # 生成 1 秒的模拟音频 (16kHz, Mono)
        python -c "import numpy as np; from scipy.io import wavfile; data=np.zeros(16000, dtype=np.int16); wavfile.write('assets/examples/SP01_001.wav', 16000, data)"
        
        # ⚠️ 关键修改：使用 Lexicon 中肯定存在的词，防止 OOV 报错
        # 如果你的词典是中文音素，就写对应的拼音或音素；如果是英文，写简单单词
        # 这里建议写一个最通用的单词，或者根据你 lexicon 的内容来
        echo "hello" > assets/examples/SP01_001.txt
        echo "✅ Mock assets (hello) generated successfully."

    - name: Run Pytest
      env:
        # 核心：从 GitHub Secrets 中安全注入你的 HF 令牌
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
        # GitHub Actions 运行环境直连原站鉴权最稳
        HF_ENDPOINT: https://huggingface.co
        # 强制 CPU 推理模式，防止云端找不到 GPU 报错
        CUDA_VISIBLE_DEVICES: ""
      run: |
        # 执行点火测试
        # 增加了 -v (详细) 和 -s (显示输出)，方便我们在 Actions 日志里看模型下载进度
        pytest -v -s tests/test_victory_lap.py