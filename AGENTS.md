# 项目协作规则

本文件适用于本仓库及其所有子目录。

## 文档语言

1. 根目录 `README.md` 是准备对外发布的项目接口，保持现有中英双语结构；不得在没有
   用户明确要求时改成纯中文或纯英文。
2. 给项目维护者和 agent 使用的文档一律使用中文，包括治理、状态、决定、未决问题、
   计划、验收、资源报告、reference 说明、测试策略和 fixture 说明。
3. 命令、代码、API/类/函数名、路径、环境变量、错误码、状态值、schema 字段以及
   必须精确匹配的标签保持原文，不为追求全中文而改写技术接口。
4. 新建内部 Markdown 时默认使用中文。修改既有内部文档时，新增内容也必须使用中文。
5. 如需改变公共 README 的语言接口或把内部文档改为其他语言，必须先取得用户明确
   决定，并在 `DECISIONS.md` 中记录。

## 事实与授权

- 项目事实权威顺序和冲突处理以 `project.md` 为准。
- `STATE.md` 记录已验证状态；`DECISIONS.md` 记录已接受决定；
  `OPEN_QUESTIONS.md` 记录未决项。
- GitHub 历史替代、远端 push、默认分支修改、tag、release 和 PyPI 上传均需要各自
  的明确授权，不得从策略决定推导出执行权限。
